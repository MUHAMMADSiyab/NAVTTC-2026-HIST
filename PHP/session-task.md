## TODO

- Create a login form (username, password)

- User clicks the submit button

- Send a request to backend (optional)

- Validate the form fields

- Set random id and isLoggedIn flag into the session

- Create a homepage.php (protected route)

- Check if userId and isLoggedIn is set in session, if they exist, allow the user to stay on the page, otherwise, redirect the user the login page

- If user is logged in, show the userId and welcome message.
