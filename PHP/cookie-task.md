## TODO

- Create a simple form (checkboxes: Light/Dark)
- User checks a checkbox
- Submit the form
- Get what user submitted
- Set the user preference of theme in cookies
- Create a demo.php file
- Check the cookie, if it exists, get the user preference from the cookie and apply a class (for instance: `dark`) on body element.
- In demo.php, create a CSS class `dark` and apply some dark(ish) styles on the elements.
